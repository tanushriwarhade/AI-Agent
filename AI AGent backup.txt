
User
do you remember we had a coversatiton about building an ai agent for Google DeepMind - Vibe Code with Gemini 3 Pro in AI Studio this competition  and you have give me prompts for that
Model
ThinkingThoughts
Expand to view model thoughts

chevron_right
While I don't have a persistent memory of our specific past conversations (each session is a fresh start for me), I am completely up to speed on the Google DeepMind - Vibe Code with Gemini 3 Pro competition!
It is huge right now—running from December 5th to December 12th, 2025, with that massive $500,000 prize pool.[1]
Since you are building an AI Agent, you are hitting the sweet spot of Gemini 3 Pro's capabilities (specifically its "Deep Think" and agentic reasoning).[2] "Vibe Coding" is all about using natural language to skip the boilerplate and get straight to a working, complex app.[2][3]
Here is a strategy and a set of "Vibe Coding" prompts to get your agent built, refined, and ready for submission in Google AI Studio.
The Strategy: "The Agentic Loop"
Gemini 3 Pro excels at planning -> critiquing -> executing.[1] Don't just ask it to "write code." Ask it to be an architect first.
Your Workflow in AI Studio:
Vibe Check (System Instruction): Define the "Soul" of the app.[3]
The Logic Layer: Force it to use "Deep Think" to plan the agent's state machine.
The UI Layer: Vibe code the interface (Gemini 3 is great at generating React/Tailwind artifacts instantly).[3]
The Refinement: Iterative conversational prompts to fix bugs.
Phase 1: The "Mega-Prompt" (Paste this into AI Studio)
This prompt uses "Vibe Coding" principles—it focuses on behavior and feeling rather than strict syntax, letting Gemini 3 handle the implementation.
Prompt:
"I want to Vibe Code an autonomous AI Agent application. The vibe is 'futuristic, clean, and highly functional dashboard.'
The Core Concept:
Build a specific Agent designed to [INSERT YOUR IDEA HERE, e.g., 'Optimize Supply Chains' or 'Plan Travel Itineraries'].
Technical Requirements:
Tech Stack: Use React for the frontend with Tailwind CSS for styling.[3] Use a clean, dark-mode aesthetic with neon accents (Cyberpunk but professional).
Agentic Logic: The app must simulate an 'Agent Loop'. It should have a visible 'Thinking' state where it breaks down a user request into steps, executes them, and shows the results.
Input: A central chat interface where the user gives a goal.
4.[3] Output: A dynamic dashboard that updates as the agent 'works'. If the agent finds data, it renders a chart. If it makes a plan, it renders a checklist.
Reasoning: Use a simulated 'Chain of Thought' display on the right sidebar so I can see the agent 'thinking' in real-time.
Vibe Instructions:
Make it feel responsive and alive.
When the agent is processing, show cool loading animations.
The code should be modular; separate the UI components from the agent logic.
Go ahead and scaffold the full application now."
Phase 2: Specific Feature Prompts (Iterate with these)
Once the base is built, use these prompts to make it competitive.
1. For "Deep Reasoning" (The 'Wow' Factor)
"Update the 'Thinking' sidebar.[3] Instead of just showing text, I want it to visually represent Gemini 3 Pro's 'Deep Think' mode. Create a collapsible tree diagram that branches out as the agent considers different possibilities. If the agent hits a dead end in its logic, show that branch turning red and collapsing. Make it look like a high-tech decision tree."
2. For Multimodal Capabilities (Gemini 3 Strength)
"Let's use Gemini 3's multimodal power. Add a drag-and-drop zone in the chat interface. If I drop a PDF or an image of a dataset, the Agent should automatically detect the file type, scan it, and generate a summary card in the dashboard before asking me what to do with it.[3] The vibe should be 'instant analysis'."
3. For "Self-Correction" (Agentic Behavior)
"Implement a 'Self-Correction' loop. If the agent generates an output that seems low confidence, I want it to spawn a 'Critic' modal that pops up and says, 'Wait, I need to double-check this.' Then, have it run a second pass and update the UI with a 'Verified' badge. Make this interaction feel seamless and trustworthy."
Phase 3: The Polish (Crucial for Judges)
The "Vibe Code" judges are looking for apps that feel complete.[3]
Prompt for Polish:
"Review the current UI. It looks a bit too static.
Micro-interactions: Add hover effects to all cards. When a new item is added to the dashboard, animate it in (slide up + fade in).
Typography: Switch the font to something monospaced for the data elements (like 'JetBrains Mono' or 'Fira Code') to enhance the 'coder' vibe, but keep the headings clean sans-serif (Inter or Roboto).
Mobile Responsiveness: Ensure the sidebar collapses into a hamburger menu on smaller screens.
Fix these styling issues and regenerate the CSS."
Quick Tips for the Competition:
Enable "Publish to Web": In AI Studio, you must publish the app for the judges to see it.[3]
Context Window: Gemini 3 Pro has a 1M context window.[1][2][3][4] Don't be afraid to paste in massive documentation or datasets if your agent needs a "knowledge base."
Video Demo: The submission usually helps if you have a screen recording.[3] Use the app to generate its own script for the video! (Prompt: "Write a 30-second script explaining this app's features to a hackathon judge.")
Do you have a specific domain you want this Agent to focus on (e.g., Finance, Coding, Health)? I can tailor the "Core Concept" prompt to fit that exactly.
Sources
help
techau.com.au
aibuilders.academy
deepmind.google
viblo.asia
Google Search Suggestions
Display of Search Suggestions is required when using Grounding with Google Search. Learn more
Google logo
Model
ThinkingThoughts
Expand to view model thoughts

chevron_right
Model
ThinkingThoughts
Expand to view model thoughts

chevron_right
Model
ThinkingThoughts
Expand to view model thoughts

chevron_right
Model
ThinkingThoughts
Expand to view model thoughts

chevron_right
Model
ThinkingThoughts
Expand to view model thoughts

chevron_right
Model
ThinkingThoughts
Expand to view model thoughts

chevron_right
This is a solid foundation, but to make it "Award-Winning" and ensure it has the Best Design, we need to upgrade that prompt to include the interactive elements we discussed (The Liquid Orb, The Gatekeeper, and The Soundscapes).
If you just use the prompt you pasted, you will get a static, boring dashboard.
I have generated the complete, merged code for you below.
This code includes:
The "Liquid Orb": A living, breathing animation in the sidebar that changes shape and color based on your energy.
The "Gatekeeper": It physically locks the calendar if your energy is low.
The "Soundscape": A visual audio player that reacts to the vibe.
Glassmorphism: High-end frosted glass UI.
You can copy and paste this entire block directly into an App.js file (or a React sandbox like StackBlitz/CodeSandbox) to see the final winning result.
The "FlowState" Winning Code (React + Tailwind)
code
JavaScript
import React, { useState, useEffect, useRef } from 'react';
import { 
  Calendar, 
  Mic, 
  Activity, 
  CheckCircle, 
  Clock, 
  ShieldAlert, 
  Music, 
  Volume2,
  BrainCircuit,
  Zap
} from 'lucide-react';

const App = () => {
  // --- STATE MANAGEMENT (The "Brain") ---
  const [energyLevel, setEnergyLevel] = useState(85); // 0-100
  const [mood, setMood] = useState('energetic'); // 'energetic' | 'drained'
  const [isListening, setIsListening] = useState(false);
  const [agentThought, setAgentThought] = useState("System initialized. Monitoring user parameters...");
  const [showGatekeeperModal, setShowGatekeeperModal] = useState(false);
  
  // --- THE LOGIC (The "Vibe" Engine) ---
  
  // Simulated Voice Analysis
  const handleVoiceInteraction = () => {
    setIsListening(true);
    setAgentThought("Listening to user voice input...");
    
    // Simulate processing delay
    setTimeout(() => {
      setIsListening(false);
      
      // Toggle state for demo purposes (Cycle between High/Low energy)
      if (mood === 'energetic') {
        // Switch to DRAINED
        setMood('drained');
        setEnergyLevel(25);
        setAgentThought("Analysis: Detected Keywords 'tired', 'overwhelmed'. Tone: Flat. -> ACTION: Activating Gatekeeper Mode.");
      } else {
        // Switch to ENERGETIC
        setMood('energetic');
        setEnergyLevel(90);
        setAgentThought("Analysis: Detected Keywords 'ready', 'focus'. Tone: Upbeat. -> ACTION: Optimizing for Deep Work.");
      }
    }, 2000);
  };

  // Calendar Click Handler (The Gatekeeper)
  const handleDateClick = () => {
    if (mood === 'drained') {
      setShowGatekeeperModal(true);
      setAgentThought("INTERVENTION: Blocked user from adding task due to Critical Energy Levels.");
    } else {
      setAgentThought("Opening schedule for new task entry...");
      // Logic to open normal modal would go here
    }
  };

  // --- STYLES (The "Look") ---
  
  // Dynamic Theme Colors
  const accentColor = mood === 'energetic' ? 'text-cyan-400' : 'text-amber-400';
  const glowColor = mood === 'energetic' ? 'shadow-cyan-500/50' : 'shadow-amber-500/50';
  const orbColor = mood === 'energetic' ? 'bg-cyan-500' : 'bg-amber-600';
  const borderColor = mood === 'energetic' ? 'border-cyan-500/30' : 'border-amber-500/30';

  return (
    <div className={`min-h-screen bg-slate-950 text-white font-sans selection:bg-cyan-500/30 flex overflow-hidden transition-colors duration-1000 ease-in-out`}>
      
      {/* --- LEFT SIDEBAR (Profile & Orb) --- */}
      <aside className="w-1/5 border-r border-white/10 bg-white/5 backdrop-blur-xl flex flex-col p-6 gap-8 relative z-10">
        <div className="flex items-center gap-3">
          <div className="w-8 h-8 rounded-lg bg-gradient-to-br from-cyan-400 to-purple-600"></div>
          <h1 className="font-bold text-xl tracking-tight">FlowState</h1>
        </div>

        {/* THE LIQUID ORB (The Soul) */}
        <div className="flex flex-col items-center justify-center flex-1">
          <div className="relative w-48 h-48 flex items-center justify-center">
            {/* Animated CSS Orb */}
            <div 
              className={`absolute w-full h-full rounded-full opacity-60 blur-2xl transition-all duration-1000 ${orbColor} animate-pulse`}
            ></div>
             <div 
              className={`relative w-32 h-32 ${orbColor} transition-all duration-1000 rounded-full flex items-center justify-center shadow-[0_0_40px_rgba(0,0,0,0.5)]`}
              style={{
                animation: isListening 
                  ? 'pulse 0.5s infinite alternate' 
                  : mood === 'energetic' 
                    ? 'morph 5s infinite ease-in-out' // Fast morph
                    : 'morph 12s infinite ease-in-out', // Slow lava morph
                borderRadius: '60% 40% 30% 70% / 60% 30% 70% 40%'
              }}
            >
              <Zap className={`w-12 h-12 text-white ${mood === 'energetic' ? 'opacity-100' : 'opacity-50'}`} />
            </div>
            
            {/* Battery Indicator Ring */}
            <svg className="absolute w-48 h-48 -rotate-90">
              <circle cx="96" cy="96" r="88" stroke="currentColor" strokeWidth="2" className="text-white/10" fill="none" />
              <circle 
                cx="96" cy="96" r="88" 
                stroke="currentColor" 
                strokeWidth="4" 
                className={`${accentColor} transition-all duration-1000`}
                strokeDasharray="552"
                strokeDashoffset={552 - (552 * energyLevel) / 100}
                strokeLinecap="round"
                fill="none" 
              />
            </svg>
          </div>
          
          <div className="mt-6 text-center">
            <h2 className="text-2xl font-bold">{energyLevel}%</h2>
            <p className="text-sm text-slate-400 uppercase tracking-widest text-xs">Energy Battery</p>
          </div>
        </div>

        {/* Controls */}
        <div className="space-y-4">
          <button 
            onClick={handleVoiceInteraction}
            className={`w-full py-4 rounded-xl flex items-center justify-center gap-3 font-semibold transition-all duration-300 ${isListening ? 'bg-red-500 scale-95' : 'bg-white/10 hover:bg-white/20'}`}
          >
            <Mic className={isListening ? 'animate-ping' : ''} size={20} />
            {isListening ? 'Listening...' : 'Update Vibe'}
          </button>
          
          {/* Ambient Sound Player */}
          <div className={`p-4 rounded-xl border border-white/5 bg-black/20 flex items-center gap-3 transition-opacity duration-500 ${mood === 'drained' ? 'opacity-100' : 'opacity-60'}`}>
            <Music size={16} className={accentColor} />
            <div className="flex-1 overflow-hidden">
              <p className="text-xs text-slate-400">Ambient Sound</p>
              <p className="text-sm font-medium whitespace-nowrap">
                {mood === 'energetic' ? 'Deep Focus Lo-Fi' : 'Rain & Theta Waves'}
              </p>
            </div>
            <Volume2 size={16} className="text-slate-500" />
          </div>
        </div>
      </aside>

      {/* --- CENTER PANEL (Calendar) --- */}
      <main className="flex-1 p-8 flex flex-col gap-6 relative overflow-y-auto">
        <header className="flex justify-between items-end">
          <div>
            <h2 className="text-3xl font-bold">Weekly Schedule</h2>
            <p className="text-slate-400">October 24 - 30</p>
          </div>
          <div className={`px-4 py-1 rounded-full border ${borderColor} ${accentColor} bg-white/5 text-sm`}>
            Mode: {mood === 'energetic' ? '🚀 High Performance' : '🛡️ Preservation'}
          </div>
        </header>

        {/* The Grid */}
        <div className={`flex-1 grid grid-cols-1 gap-4 transition-all duration-700 ${mood === 'drained' ? 'grayscale opacity-80' : ''}`}>
          {['09:00 AM', '11:00 AM', '02:00 PM', '04:00 PM'].map((time, i) => (
            <div 
              key={i} 
              onClick={handleDateClick}
              className="group p-4 rounded-xl bg-white/5 border border-white/5 hover:border-white/20 cursor-pointer transition-all flex gap-4 items-center"
            >
              <span className="text-sm text-slate-500 font-mono">{time}</span>
              <div className="h-full w-0.5 bg-white/10 group-hover:bg-cyan-500/50 transition-colors"></div>
              {i === 1 ? (
                <div className={`flex-1 p-3 rounded-lg ${mood === 'energetic' ? 'bg-cyan-500/20 border border-cyan-500/30' : 'bg-slate-800 border border-slate-700'}`}>
                  <h4 className="font-semibold">Team Sync</h4>
                  {mood === 'drained' && <span className="text-xs text-amber-400">⚠ Suggest Rescheduling</span>}
                </div>
              ) : (
                <div className="text-slate-600 text-sm italic">Empty Slot (Click to add)</div>
              )}
            </div>
          ))}
        </div>

        {/* GATEKEEPER MODAL */}
        {showGatekeeperModal && (
          <div className="absolute inset-0 bg-slate-950/80 backdrop-blur-sm z-50 flex items-center justify-center p-8 animate-in fade-in zoom-in duration-300">
            <div className="bg-slate-900 border border-amber-500/50 p-8 rounded-2xl max-w-md shadow-2xl shadow-amber-900/20 text-center">
              <ShieldAlert className="w-16 h-16 text-amber-500 mx-auto mb-4" />
              <h3 className="text-2xl font-bold text-white mb-2">Energy Critical (25%)</h3>
              <p className="text-slate-400 mb-6">
                I cannot allow you to book new meetings right now. Your voice pattern indicates high stress.
              </p>
              <div className="bg-white/5 p-4 rounded-lg text-left mb-6">
                <p className="text-xs text-slate-500 mb-1">DRAFTED REPLY:</p>
                <p className="text-sm italic text-amber-200">"I'm at capacity for today. Let's look at next week."</p>
              </div>
              <button 
                onClick={() => setShowGatekeeperModal(false)}
                className="px-6 py-2 bg-amber-600 hover:bg-amber-500 rounded-lg font-medium transition-colors"
              >
                Acknowledge & Rest
              </button>
            </div>
          </div>
        )}
      </main>

      {/* --- RIGHT SIDEBAR (Knowledge & Agent Log) --- */}
      <aside className="w-[30%] bg-black/20 backdrop-blur-md border-l border-white/5 p-6 flex flex-col gap-6">
        
        {/* Agent Thought Stream */}
        <div className="bg-slate-900/50 p-4 rounded-xl border border-white/10 min-h-[150px]">
          <div className="flex items-center gap-2 mb-3 text-xs font-bold tracking-wider text-slate-500 uppercase">
            <BrainCircuit size={14} /> Agent Reasoning
          </div>
          <p className="font-mono text-sm text-cyan-300/90 leading-relaxed typing-effect">
            {">"} {agentThought}
          </p>
        </div>

        {/* Task Hub */}
        <div className="flex-1">
          <h3 className="font-bold mb-4 flex items-center gap-2">
            <CheckCircle size={18} className="text-slate-400" /> Tasks
          </h3>
          <div className="space-y-2">
            {[1, 2, 3].map((task) => (
              <div key={task} className="flex items-center gap-3 p-3 rounded-lg hover:bg-white/5 transition-colors group">
                <div className={`w-4 h-4 rounded-full border-2 ${borderColor} group-hover:bg-cyan-500/20`}></div>
                <div className="flex-1">
                  <div className="h-2 w-24 bg-slate-800 rounded mb-1 group-hover:w-32 transition-all"></div>
                  <div className="h-1.5 w-12 bg-slate-800/50 rounded"></div>
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* Stats */}
        <div className="grid grid-cols-2 gap-4">
          <div className="p-4 bg-white/5 rounded-xl border border-white/5">
            <Activity className="text-slate-400 mb-2" size={20} />
            <div className="text-2xl font-bold">4h</div>
            <div className="text-xs text-slate-500">Deep Work</div>
          </div>
          <div className="p-4 bg-white/5 rounded-xl border border-white/5">
            <Clock className="text-slate-400 mb-2" size={20} />
            <div className="text-2xl font-bold">2</div>
            <div className="text-xs text-slate-500">Meetings Left</div>
          </div>
        </div>
      </aside>
      
      {/* Global CSS for Morph Animation */}
      <style>{`
        @keyframes morph {
          0% { border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%; }
          50% { border-radius: 30% 60% 70% 40% / 50% 60% 30% 60%; }
          100% { border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%; }
        }
      `}</style>
    </div>
  );
};

export default App;
Model
ThinkingThoughts
Expand to view model thoughts

chevron_right
Model
ThinkingThoughts
Expand to view model thoughts

chevron_right
Model
ThinkingThoughts
Expand to view model thoughts

chevron_right
Model
ThinkingThoughts
Expand to view model thoughts

chevron_right
To make the Agent talk back to you, the easiest and most effective way for a competition demo is to use the browser's native Web Speech API (speechSynthesis).
It requires no API keys, costs zero dollars, and works instantly in Chrome (and StackBlitz).
Here is how to add the Voice Output to your app.
Option A: The Prompt (If asking Gemini to update code)
If you are generating the code in AI Studio, send this prompt:
"Update the code to add Text-to-Speech (TTS) capabilities.
Create a helper function called speak(text) using window.speechSynthesis.
Vibe Check: Set the voice pitch to 0.8 (slightly deeper) and rate to 1.0 so it sounds like a calm, futuristic AI.
Triggers:
When the Gatekeeper blocks a meeting, make the Agent say: 'I cannot allow that. Your energy is too low.'
When I switch modes, make the Agent say: 'Optimizing interface for deep work' or 'Switching to recovery mode.'
Add a generic 'Mute' state so it doesn't talk if I don't want it to."
Option B: The Code (Paste this into StackBlitz)
If you are already in StackBlitz using the code I gave you earlier, you can manually add this feature in 30 seconds.
1. Add this function inside your App component (near the top):
code
JavaScript
// --- TEXT TO SPEECH ENGINE ---
const speak = (text) => {
  // Check if browser supports speech
  if ('speechSynthesis' in window) {
    // Cancel any current speaking to avoid overlap
    window.speechSynthesis.cancel();
    
    const utterance = new SpeechSynthesisUtterance(text);
    
    // Get available voices and try to pick a good one
    const voices = window.speechSynthesis.getVoices();
    // Try to find a "Google US English" or generic English voice
    const preferredVoice = voices.find(voice => voice.name.includes('Google US English')) || voices[0];
    
    utterance.voice = preferredVoice;
    utterance.pitch = 0.9; // Lower = more "AI" sounding
    utterance.rate = 1.05; // Slightly faster
    utterance.volume = 1;
    
    window.speechSynthesis.speak(utterance);
  }
};
2. Update your handleVoiceInteraction function:
Find the part where you switch moods and add the speak() command.
code
JavaScript
const handleVoiceInteraction = () => {
  setIsListening(true);
  setAgentThought("Listening to user voice input...");
  
  setTimeout(() => {
    setIsListening(false);
    
    if (mood === 'energetic') {
      setMood('drained');
      setEnergyLevel(25);
      setAgentThought("Analysis: Stress detected. -> ACTION: Activating Gatekeeper Mode.");
      
      // ADD THIS LINE 👇
      speak("High stress detected. I am activating Gatekeeper protocols to protect your health.");
      
    } else {
      setMood('energetic');
      setEnergyLevel(90);
      setAgentThought("Analysis: Energy high. -> ACTION: Optimizing for Deep Work.");
      
      // ADD THIS LINE 👇
      speak("Systems charged. Optimizing schedule for high performance.");
    }
  }, 2000);
};
3. Update your handleDateClick function:
Find the part where the modal opens.
code
JavaScript
const handleDateClick = () => {
  if (mood === 'drained') {
    setShowGatekeeperModal(true);
    setAgentThought("INTERVENTION: Blocked user from adding task due to Critical Energy Levels.");
    
    // ADD THIS LINE 👇
    speak("Access denied. You are running on reserve power. Please rest.");
    
  } else {
    setAgentThought("Opening schedule for new task entry...");
  }
};
Why this wins the "Vibe" category:
When the judges watch your video, they will see you click a button, and the app will verbally scold you for trying to work while tired.
That isn't just code; that's a character. That is exactly what Google DeepMind is looking for in an "Agent."
